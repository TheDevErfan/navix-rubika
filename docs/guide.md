# راهنمای جامع استفاده از Navix

به مستندات کتابخانه `navix-rubika` خوش آمدید. این کتابخانه برای توسعه سریع و قدرتمند ربات‌های روبیکا طراحی شده است.

## شروع سریع
برای شروع، کافی است کلاینت و دیسپچر را مقداردهی کنید:
```python
from navix import Client, Router, Dispatcher

client = Client(token="YOUR_TOKEN")
router = Router()
dp = Dispatcher(client, router)
cat << 'EOF' > CODE_OF_CONDUCT.md
# مرام‌نامه جامعه کاربری Navix

ما متعهد به ایجاد فضایی امن، دوستانه و بدون تبعیض برای تمام توسعه‌دهندگان هستیم. هرگونه رفتار توهین‌آمیز، تبعیض‌آمیز یا آزاردهنده در بخش‌های مختلف پروژه (Issues, Pull Requests و غیره) به هیچ وجه پذیرفته نیست و فرد خاطی مسدود خواهد شد.
